# BITE LAB — Agent 工作约定

## 工作目标

在保持物理含义、事务回滚和可复现验证的前提下改进模拟器。不得为了“看起来能跑”而隐藏模型失效。

## 修改规则

1. 先定位根因，再修改；诊断请求不自动授权改物理。
2. 物理、几何、绘制、读数和缓存状态必须使用同一语义。
3. 用户输入的前角表示最终装配后的物理 rake，不能因单轴镜像改变正负语义。
4. 叉子铰点仅由相对武器原点的 X/Y 决定；初始姿态必须使叉尖直接触地，不能开算后再自由下落。
5. DXF 无法无损解析时必须拒绝并提示，禁止静默拉直或丢弃实体。
6. 材料模型不可信时保留安全停止；禁止直接打开 `TRACEABLE_CUTTING_CONTINUOUS_TOI_ENABLED`。
7. 不使用经验冲量折扣、速度上限、按齿数缩放、位置投影或 PID 式参数来修接触问题。
8. 不放宽 0.08 mm 硬门；无效尝试必须回滚，不能进入播放缓存。
9. 高级面板只控制显示，不得改变同一参数下的物理结果。
10. 数字输入在编辑时允许空白；仅在提交时验证，并恢复最近有效值。

## 物理修改最低验证

- CAD/CAD、参数齿/CAD叉、CAD武器/参数叉、参数/参数四组合。
- 1、10、20 齿及多个传动比和初始角。
- 武器—靶、叉—靶、靶—地、叉—地持续多点接触。
- 已接受侵入不超过 0.08 mm；越界尝试必须回滚。
- 接触最终自然分离，或给出明确模型/求解域停止原因。
- 动量、机械能、材料功、质量、质心和惯量账一致。
- 取消预计算、单步、随机 seek 和缓存回放保持确定。
- 异常路径恢复世界、时间、碰撞组、账本和内部求解器状态。

## 发布规则

- `index.html` 是入口；所有运行依赖必须保存在本地目录。
- 发布目录不得包含 QA profile、临时报告或浏览器缓存。
- 用户提供的 DXF/DWG 只能复制，不能移动或覆盖原文件。
- 更新程序后同步版本号、缓存参数、使用说明和已知问题。
- 不得把“理想刚体上限”描述成真实材料预测。

