# BITE LAB：问题审计与使用结论

> 审计日期：2026-08-17  
> 范围：只读审查、浏览器实际启动与短轨迹验证；未修改模拟器源代码。

## 快速使用

1. 保留整个解压目录（特别是 `assets/`），直接打开 `index.html`，无需安装依赖。
2. 等待右上角状态显示“Rapier SIMD 已就绪”或“Rapier 兼容后端已就绪”。
3. 当前建议只把内置 **1.36 kg** 配置作为可直接运行的起点；其余量级预设在修复质量参数前不可用于可信比较。
4. 自定义模型需分别导入武器和叉子的 ASCII DXF，设置单位、镜像，并在画布点选武器转轴。
5. DXF 设计边界为：`LINE`、`ARC`、`CIRCLE`、无 bulge 的二维 `LWPOLYLINE` / `POLYLINE`；DWG 应先转换为 ASCII DXF。
6. 若要查看理想刚体碰撞趋势，选择“理想刚体上限对照”；默认“材料”模式会在首次承载武器接触时安全回滚并停止，这不是计算卡死，也不能用刚体结果当真实切削预测。
7. 设置计算时长后点击播放。程序会先预计算，再可暂停、单步或拖动时间轴回放；建议先用 `0.05–0.2 s` 试跑。

## 已确认问题

### P1：多点接触流形可能读取错误的冲量

- 位置：`app.js:2223`、`app.js:2226`、`app.js:2227`。
- 原因：代码已把 raw contact 映射到 `mappedSolverIndex`，并正确用该索引读取 solver point/distance；但读取 `contactImpulse` 与 `contactTangentImpulse` 时仍传入 raw `contactIndex`。
- 动态证据：默认刚体工况 1 秒中出现 1,208 个 `numContacts=2`、`numSolverContacts=1` 的流形；存在 solver point 对应 raw[1] 而非 raw[0] 的情况。
- 影响：承载接触时可能错记或漏记冲量、牙齿/刀体分类、命中次数和能量账。
- 建议修复：在 `hasSolverContact` 分支中改用：

  ```js
  manifold.contactTangentImpulse(mappedSolverIndex)
  manifold.contactImpulse(mappedSolverIndex)
  ```

  raw index 仅继续用于原始几何的 `contactDist` / 局部接触点读取。

### P1：多数“量级预设”不能建立有效刚体，或沿用错误质量

- 位置：`app.js:407`、`app.js:6857`、`app.js:1549`。
- 原因：除 1.36 kg 外，`ROBOT_PRESETS` 基本未显式提供 `weaponMass` / `forkMass`；`applyRobotPreset()` 只复制预设中已有字段，因此保留了默认武器质量 `0.1483278 kg` 和叉子质量 `0.03 kg`。
- 动态复现：
  - 150 g：武器 + 叉子 = `0.1783 kg`，大于整机 `0.15 kg`，进入 `invalid-rig`。
  - 5 lb、13.6 kg、110 kg：输入惯量超过遗留质量与有效半径所允许的 `I ≤ mR²`，进入 `invalid-rig`。
  - 220 g、454 g：可以建场，但继续使用 148 g 武器与 30 g 叉，底盘质量预算错误，结果不代表所选量级。
- 建议修复：每个预设提供可审计的 `weaponMass` 与 `forkMass`；数据缺失时应进入“待补质量”状态并阻止求解，而不是沿用上一套参数。不要用 `I/R²` 自动补质量。

### P1：DXF 导入会静默改变部分不受支持的几何

- 位置：`app.js:524`、`app.js:893`、`app.js:6974`。
- 3D 压平：DXF 解析器对 `LINE` 等实体只读取 XY 坐标，忽略 Z 坐标、elevation 和 extrusion/normal 信息。实测一个带 Z 坐标的三角形 DXF 被成功导入并建立 2D 刚体，而不是拒绝。
- 小缺口补边：`pointNear()` 的闭合容差为 `0.00035 m`（0.35 mm），`buildCollisionLoops()` 会把端点相距 0.2 mm 的开口轮廓识别为闭合实体。实测该图纸被成功建立为刚体碰撞体。
- 影响：导入轮廓、碰撞体和显示的 CAD 几何可能与原图不一致，违反“无法无损解析时应拒绝”的模型约定。
- 建议修复：
  - 对非零 Z/elevation、非 XY 平面 extrusion/normal、3D 实体直接拒绝；
  - 对开口轮廓或端点不精确闭合的文件报错并提示用户修图，不要静默补边；
  - 若确需数值容差，应小于几何安全门并向用户明确披露。

### P2：套用预设后的靶子初始位置依赖上一段仿真状态

- 位置：`app.js:1083`、`app.js:6857`。
- 原因：`applyRobotPreset()` 在重置仿真前调用 `getShovelFrontWorld()`；后者使用当前 `state.sim` 的叉子世界位置与姿态。
- 动态复现：全新套用 110 kg 时靶子 X 为 `690 mm`；先运行默认模型 50 ms 后再套用同一预设，靶子 X 变为 `845.208 mm`。
- 影响：同一预设会因操作历史而产生不同初态、接触时刻和结果，削弱可复现性。
- 建议修复：基于新参数下的初始装配姿态计算前缘，或先重置世界再计算靶位；不要读取旧 `state.sim`。

### P2：默认刚体示例的第一次武器接触不是牙齿命中

- 动态结果：在“理想刚体上限对照”下，默认配置首次武器接触为刀体/背板碰撞：`toothHitCount=0`、`bodyImpactCount=1`、`toothOrder=null`。
- 影响：若默认示例的目标是演示单齿 bite，其初始相位或靶位与该目标不一致，容易误导用户。
- 建议修复：调整默认初始角度/靶位，或把默认场景明确标注为非牙齿碰撞示例；加入“首次接触必须为牙齿”的回归测试。

## 已知模型边界（不是新增 bug）

- 默认 `contactModel: material` 的连续切削/材料去除安全门尚未启用；首次承载武器接触会回滚到安全帧并停止，不会输出真实切削量。
- “理想刚体上限对照”可用于几何和不可变形碰撞趋势比较，但不等同真实材料、裂纹、切削、碎片或击飞预测。
- 底盘只具有水平自由度；不包含整机起跳、俯仰、悬架、载荷转移和三维偏载。
- 完整 4 秒轨迹可能较慢；固定物理步长为 0.5 ms。

## 验证记录

- `node --check` 已通过应用与本地 JS 资源。
- Chromium 无头模式可直接打开 `file://.../index.html`，默认 Rapier SIMD 后端正常初始化；强制 SIMD 初始化失败时会回退到兼容后端。
- 默认 CAD/CAD 刚体场景、DXF 重导入、快照恢复、CAD/参数几何组合与短轨迹缓存已做运行验证。
- 仓库没有 `package.json`、构建配置或自动化测试配置；当前没有现成测试套件可替代上述浏览器验证。
