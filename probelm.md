# BITE LAB：问题审计、修复方案与结果

> 初始审计：2026-08-17
>
> 修复复核：2026-08-19
>
> 基线：Git 提交 `9e835c3`，注释标签 `v1.0`
>
> 当前程序版本：`1.1.0`

## 如何使用

1. 保留整个目录，直接打开 `index.html`；运行依赖均在本地 `assets/` 中。
2. 等待状态显示“Rapier SIMD 已就绪”或“Rapier 兼容后端已就绪”。
3. 内置 1.36 kg 配置可直接运行。其他量级会进入“待补质量”；填写武器完整旋转组件质量和活动叉质量后才会启用播放/单步。
4. 自定义模型分别导入武器和叉子的 ASCII DXF。成功导入会清空对应部件质量，失败导入不会改变当前图纸、质量或仿真状态。
5. DXF 只接受严格二维的 `LINE`、`ARC`、`CIRCLE`、无 bulge 的 `LWPOLYLINE` / `POLYLINE`，并且拓扑必须是一个外环加普通孔。
6. 默认“有限材料”模式在首次承载武器接触前事务回滚并安全停止；这不是卡死，也不能把“理想刚体上限对照”当成真实切削预测。
7. 建议先用 `0.05–0.2 s` 计算时长试跑，再按需要延长到 4 s。

## 审计结论与修复状态

### 已排除：raw / solver contact 索引并非冲量读取错误

Rapier 的 `contactImpulse(i)` / `contactTangentImpulse(i)` 使用几何 contact 索引；`solverContactPoint(i)` / `solverContactDist(i)` 使用 solver contact 索引。把冲量改读 solver 索引反而会错配。

本次保留正确语义，并完成以下加固：

- 抽出一对一的几何 contact → solver contact 匹配函数；
- 字段明确区分 `geometricContactIndex` 与 `solverContactIndex`；
- 为非恒等映射、未匹配 contact 和稳定排序加入浏览器回归。

### 已修复：量级预设继承错误质量

除内置 1.36 kg 外，仓库没有可审计的武器/叉子质量，不能凭尺寸级预设编造。本次采用“缺数据即阻止求解”的方案：

- 每个预设显式携带质量状态和来源；
- 非 1.36 kg 预设清空 `weaponMass` / `forkMass`，公开状态为 `needs-input`；
- 播放和单步禁用，补齐两个正有限质量后恢复 `rapier-rig`；
- 不继承上一预设，不用 `I/R²` 反推质量；
- 新导入武器/叉子会清空对应质量；重新套用 1.36 kg 也不会把内置质量灌入自定义图纸。
- 预设按武器半径重算轴高后会校验完整刀/叉装配；较小的自定义武器不会再把保留的叉子降入地面；
- 套用预设作为一个事务提交，装配失败会恢复套用前的全部参数和已应用预设标记。

### 已修复：DXF 会静默压平或补合几何

导入器现在先完成严格解析和拓扑验证，再事务提交：

- 拒绝非零 Z/elevation/thickness/width、非默认 `+Z` extrusion、3D/网格实体和非数值组码；
- 拒绝 bulge、未知轮廓几何、零长度/退化实体；
- 端点等价阈值改为 `0.0001 mm` 欧氏距离；`0.2 mm` 缺口明确报 `DXF_OPEN_CONTOUR`；
- 拒绝开放轮廓、连接歧义、自交、多个独立外环、相交孔和嵌套孔；
- 支持一个外环加普通孔，孔共同进入渲染、Earcut 碰撞体、叉子面积/质心/惯量、初始重叠和运行时 0.08 mm 穿透审计；
- 主要拒绝路径提供稳定 `DXF_*` 错误码。

### 已修复：预设初态依赖上一段仿真

旧实现用当前 `state.sim` 的叉子世界姿态计算新靶位。同一 110 kg 预设可能得到不同 X 坐标。

现在靶位只由新参数、当前图纸和解析得到的初始接地叉姿态计算，不读取旧仿真。回归会先运行默认场景，再与全新页面套用同一预设，比较靶位、叉子初始角和武器原点。

### 已修复：默认刚体示例先撞背板

内置武器初始角从 `0°` 改为 `75°`，靶子仍为 `X = 380 mm`。`65–85°` 已逐整数度验证为牙齿优先区间；`75°` 位于其中点。

修复后默认刚体首个承载武器接触为 `toothOrder = 0`，不会先增加背板计数。材料模式仍按原安全门回滚，不提交临时刚体冲量或删料。

## 修改方案

1. **先保留基线**：初始化本地 Git，以 `9e835c3` 保存原仓库并打 `v1.0` 注释标签。
2. **统一几何语义**：解析器输出“单外环 + 孔”的规范实体；渲染、碰撞、质量属性、重叠和穿透检查消费同一实体。
3. **显式输入可信度**：预设质量包含 `ready` / `needs-input` 状态，任何来源不匹配都清空而不是回退。
4. **消除历史状态**：初始装配计算只依赖参数和图纸；应用成功后一次性重建仿真。
5. **保留物理硬门**：不启用未验证的连续切削，不放宽 0.08 mm 门，不增加经验冲量/速度/PID 修正。
6. **自动化回归**：使用本地 Edge/Chrome CDP 对真实 `file://` 入口运行，不引入第三方测试依赖。

## 验证范围

- `node --check app.js` 与 QA 脚本语法检查；
- `node --test qa/dxf-fixtures.test.mjs`：15 个 ASCII DXF 夹具的目录/结构自检；
- `node qa/browser-regression.mjs`：SIMD 与 compat 启动、四种 CAD/参数组合实际接触、1/10/20 齿、多个传动比/初始角、镜像前角、快照、质量待输入、DXF 真导入、孔洞空腔、接触分类、材料回滚、能量账和缓存 seek；
- 默认刚体所有已接受的武器、叉子、靶子-地面、叉子-地面侵入保持在 `0.08 mm` 以下；
- 材料域停止时命中、删料面积/质量和材料功均为零，终止安全帧重复推进保持不变。

## 仍然存在的模型边界

- 连续塑性/断裂、沟槽历史、最早压缩 TOI 和余步重演尚未完成，`TRACEABLE_CUTTING_CONTINUOUS_TOI_ENABLED` 保持关闭。
- “理想刚体上限对照”仅用于几何和不可变形碰撞趋势，不是真实材料、裂纹、碎片或击飞预测。
- 底盘只有水平自由度，不包含整机起跳、俯仰、悬架、载荷转移和三维偏载。
- DXF 暂不支持多个独立刚体、孔中岛、SPLINE/ELLIPSE/bulge/BLOCK 投影或任意 OCS/WCS 转换；这些情况会明确拒绝。
- 完整 4 s、复杂 CAD 或持续多点接触仍可能耗时较长；固定物理步长保持 `0.5 ms`。
