# 连续材料接触相位：2026-09-11 验证记录

## 2026-09-11 增量

- 最新原型 SHA-256：`798B8696F215A7891A9E2DF17DAA6140478E9D56B4449B997BB87DBB4D220792`。
- 已加入靶体中心局部 X 坐标、基于实际 f32 写回的逐坐标动量舍入预算、变形新增区域单次暂存、直接质量几何减塑性场、过程区存活裁剪及 5 µm 边界误差带分辨率门。
- 42 项相位断言、49 项过程区账本断言、叉子动量单测和局部原点静态审计通过。正式 `app.js` 哈希仍为 `89E73B62F395719A5C48589B20922E5D839938B97454B82817E3F459F178E4C0`。
- 最后一次 r7 短程整机测试已越过旧 0.274 s 停止点，但在运行期间按用户要求中止；无完整报告，不得记为整机通过。
- 下一步是完整重跑 r7，然后才能扩展多构型、r8–r10 与长时程。

## 最新暂停点（用户要求暂停）

最新原型 SHA-256：`4F524343B8EA69BE50B7AE4B7639814B85F28D987264860A7DF4F78AF7163B33`。
下面此前的原型哈希/最终测试仅属于上一轮快照。

- 修复 `applyWeaponTargetImpulse` 把叉质心速度写成铰轴速度的问题。现在叠加 `{dv-dOmega*r.y, dOmega*r.x}`，保留已有角运动，不投影、不改门槛。
- `work/run-material-momentum-unit-qa.js` 从真实源码提取函数验证：旧写法在 3/3 个非零 COM 角度案例失败，新写法通过零冲量不变及铰点速度连续测试。
- 修复受力回放扫掠仍使用接触前自由飞行起点的问题；受力段现在使用真实 TOI 起点和本段时长，绝对时间偏移单独传入。
- 最新 `material-force-local-sweep-r6.json`：普通工况仍在 0.274 s 停止；回滚工况通过。之前叉速度单独修改的 A/B 为 `material-com-velocity-r6.json`。上述原型语法及 42 项相位断言通过。
- `work/run-rapier-position-precision-qa.js` 使用本地真实 Rapier 0.20.0 复现 f32 积分量化：x0=0.38 m、vx=0.05 m/s、总时长0.5 ms，在768/3072子步的终点误差约25 µm；vx=0.5 m/s在3072子步误差约116 µm。x0=0时误差显著下降。过度细分确实可能无法收敛，不应继续只提高细分层数。
- 动量账同时存在原始动量与增量聚合标度不一致的疑点，但尚未实现或验证舍入账本修正；不能把所有残差定性成浮点误差，叉COM错误证明原系统也有真实运动学缺陷。
- 后续优先核查精度保持方案，以及材料塑性/断裂账本；位置投影、速度帽、放宽穿透门和任意加大动量容差均不可采用。尚未开始浮动原点或双精度引擎实现。
- 暂停时子智能体均已结束，未发现本轮测试进程。正式版未改动。

---

状态：研究原型推进，材料模式仍未通过完整验收，不合并正式版。

正式版 `outputs/bite-simulator/app.js` SHA-256 仍为
`89E73B62F395719A5C48589B20922E5D839938B97454B82817E3F459F178E4C0`。
当前原型 `material-continuous-prototype-app.js` SHA-256：
`C79F806811805B57388434C30E61CF0FE2BFC62C567FE8124D04A8C6C021152C`。

## 已实现

- 在 `physics.materialEventState` 保存入口材料几何、累计唯一扫掠区域、各 CAD 顶点累计路径、`Phi=A-h_min*S` 和切屑锁存时间。路径先按顶点累加再取最大；空间塑性功账本仍独立。
- 预览为纯函数；自由和受力响应从同一根快照定位已括住的相变边界，只有接受轨迹提交时才锁存。
- 同 feature 的 chip 不允许退回 ploughing；短暂无 TOI 不清空状态，武器接触 episode 结束时清空。
- 原型增加相位、相变根和分 feature 塑性账本的双分辨率审计。跨轨迹相变时间差换算为路径误差，与已有 20 µm 路径积分门比较；单轨迹二分根精度维持原值。
- 修复能量受限删料分支刷新扫掠几何时丢失 `toiAudit` 的问题。
- 保留 0.5 ms 正式时间步、80 µm 穿透门、8192 子步上限、独立失败域和事务回滚。

## 当前源码验证

```powershell
node --check work/material-continuous-prototype-app.js
node work/run-material-engagement-phase-qa.js
node work/run-final-static-qa.js
node work/run-continuous-material-scrape-qa.js --app=work/material-continuous-prototype-app.js --suite=prototype --horizon=.28 --max-refinement=6 --parallel=1 --output=work/material-engagement-final-r6.json
```

- 语法、正式静态检查通过。
- 相位单元与根搜索测试 42 项断言通过，覆盖面积去重、等路径分区、逐顶点路径累计、预览无副作用、feature 隔离、锁存和同根最终回放。
- 故障注入通过，包括临时删料确已到达、字节级事务回滚、80 µm 门；本次两个浏览器 profile 均已自动清理。
- 普通短测失败，停止于 0.274 s，尚未到达 0.28 s；双分辨率塑性区/位姿未收敛，接受事件的动量账超差，完整接触能量尚未完成。不得把回滚通过当作材料通过。
- 旧 `run-p0-failure-domain-qa.js` 默认依赖已不存在的旧候选文件；指向当前原型后其旧函数签名锚点仍不兼容，未执行物理测试。本次采用上面的专用故障注入测试验证材料事务。

## 较高分辨率证据

`material-engagement-r8.json` 来自本轮较早快照
`6860C30967DB2AE38820E356EABC8329CF1AA3808EEA86B64C21B44919E34029`，不是最终源码。
耗时 512.162 s，仍停在 0.274 s。其第二接触固定步的 r2–r8 都继承同一个已接受的 chip 锁存时间，因此只能证明该接触的锁存持续，不能单凭此声称首次相变根在不同初始轨迹间已收敛。
r7/r8 的姿态差约 48.807 µm，塑性区面积差约 0.000115 mm²，未通过既有收敛门。

## 后续工作

1. 审计塑性过程区推进和低速同根 free/forced 响应为何在加密时改变位姿与空间账本；不放宽门槛来获得通过。
2. 单独追查接受固定步动量账残差（本轮短测约 1.4363e-6，对应门约 2.2133e-7），区分积分/浮点误差、外约束与账本定义；不能将其写成已闭合。
3. 加入先越阈值后回落、真实分离后再接触、不同 feature 交替、带孔几何和流形间隙的轨迹测试。目前根搜索针对已括住的正向跨越；全程最早根与复杂相位拓扑仍需独立验证。
4. 短测通过后再跑 CAD/参数武器和叉、多角度、多齿、r8–r10 和长时矩阵。当前不能发布为已完成的材料模式。
