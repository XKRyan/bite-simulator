# BITE LAB v1.0 — 项目交接

## 当前状态

- 浏览器端二维格斗机器人武器咬入模拟器，可直接打开 `index.html` 使用。
- 支持武器/叉子 DXF、参数测试齿、参数测试叉、镜像、动力参数、靶子自动质量、0.5 ms 单步、可选 0.0005–4 s 计算时长及缓存回放。
- Rapier 2D SIMD 优先，失败时自动回退兼容后端。
- 参数齿前角采用最终装配后的物理符号，镜像不会反转用户输入语义。
- 叉子为独立铰接刚体；参数叉是一条有质量的理想细线段，初始叉尖直接位于地面。
- 持续多点刚体接触使用六自由度联立互补求解，覆盖武器—靶子、叉子—靶子、靶子—地面和叉子—地面。
- 所有数值输入可先清空再填写；空白失焦时恢复最近有效值。

## 已完成验证

- 四种 CAD/参数武器与 CAD/参数叉组合均可建立。
- 10/20 齿刚体矩阵 64/64 通过。
- 历史刀叉夹靶难例通过，已接受几何侵入保持在 0.08 mm 硬门以内。
- 异常路径能完整回滚时间、刚体、世界、碰撞组和求解器状态。
- 材料安全停止会回滚末固定步，不提交临时刚体冲量、靶子速度或能量跳变。

## 当前主要缺口

1. 连续材料剐蹭、塑性沟槽、硬化、断裂和去料历史尚未可靠实现。
2. 材料模式在首次承载接触时安全停止；不能简单关闭安全门。
3. 理想刚体模式仅是不可变形碰撞上限，可能明显高估真实击飞。
4. 底盘只计算水平反冲，不含整机竖向跳起、俯仰、悬架和三维偏载。
5. 完整切断后的多个碎片刚体尚未支持。
6. DXF 不支持 bulge、SPLINE、ELLIPSE、INSERT 和三维 polyline；必须明确拒绝，不能静默改形。

## 下一阶段建议

优先开发连续材料接触，而不是调整冲量系数：

1. 为材料表面增加可回滚的持久状态：沟槽轮廓、塑性法向偏移、累积塑性应变/硬化、切向滑移和损伤。
2. 求最早压缩接触 TOI，在 TOI 处分裂固定步。
3. 把材料本构纳入现有六自由度联立接触求解。
4. 更新变形/去料后的剩余材料边界、质量、质心和惯量，再重演剩余时间。
5. 验证 CAD/参数齿 × CAD/参数叉 × 1/10/20 齿，并检查时间步、几何、动量、能量和质量收敛。

## 重要原则

- 不用速度限幅、按齿数缩放冲量、经验效率或 PID 式调参掩盖模型缺口。
- 不放宽 0.08 mm 几何安全门来消除停止。
- 超出已验证模型域时必须事务回滚并明确说明。
- 修改物理前先保留 v1.0 发布目录作为只读基线。

