# 武器咬入模拟器：开发交接

## 2026-09-11 状态摘要

- 正式应用仍是 `1.0.2`，`app.js` SHA-256 为 `89E73B62F395719A5C48589B20922E5D839938B97454B82817E3F459F178E4C0`，本轮材料研究未修改正式文件。
- 最新研究原型是 `work/material-continuous-prototype-app.js`，SHA-256 为 `798B8696F215A7891A9E2DF17DAA6140478E9D56B4449B997BB87DBB4D220792`。
- 材料模式仍处于严格验收前的原型阶段；不得宣称已完成，不得并入正式 `app.js`。

## 最新原型进展

- 已实现持久材料接触相位：保存入口几何、唯一累计扫掠、CAD 顶点弧长、`Phi=A-h_min*S` 和切屑锁存时间；预览无副作用，仅接受轨迹提交状态。
- 修正叉子非零质心的冲量速度写回，保留铰点速度连续；旧算法在 3/3 个倾斜案例失败，新算法通过。
- 材料强制响应的扫掠已从真实 TOI 根重演，不再错用未受力飞行起点。
- Rapier 局部坐标以靶体初始 X 为原点，显著降低 f32 大坐标积分误差；场景输入输出仍使用原坐标。
- 动量账本现按每个事件的 f32 写回舍入上界、质量/惯量及耦合系数构建逐坐标容差；代表接触的动量与能量账本已通过。
- 变形事件只暂存本次新增区域，固定步发布不再每个子片重复并入累计补集。
- 固定步直接从质量几何扣除塑性场，避免将新生的相交边界再次送入差集导致扫描树失稳。
- 后续删料会把特征塑性过程区裁剪到最终存活材料；小于已有 5 µm 扫掠边界误差带的空间碎片不再作为跨步塑性信用，其已测得功仍保留为耗散。

## 当前验证证据

- `run-material-engagement-phase-qa.js`：42 项断言通过。
- `run-material-process-ledger-qa.js`：49 项断言通过，包括空间不重叠、必需/已付功恒等、后续删料裁剪和不可分辨薄片丢弃。
- `run-material-momentum-unit-qa.js`：通过，并能检出旧叉子质心速度实现。
- `run-local-origin-qa.js --static`：通过，同时验证正式版哈希未变。
- 短程 r7 整机测试在原先 0.274 s 安全停止点后继续运行，随后按用户要求中止，因此没有完整报告，不计为通过。

## 当前风险与下一步

1. 重跑 r7 短程至自然结束，确认首个接触之后无新的失败域，并保留完整收敛报告。
2. 通过后扩展到参数/CAD 武器与叉、多角度、多齿、r8–r10 和长时程；记录几何、动量、能量、回滚与穿透门的完整证据。
3. 只有上述矩阵严格通过，才能将原型合并进正式应用并同步缓存版本、README 和本文档。
4. 项目迁移到 F 盘仍延后；完成后必须先向用户确认精确目标路径。

## 当前版本

- 当前开发版：`1.0.2`。
- 已冻结的 `v1.0` 发布副本位于项目同级的 `bite-simulator-v1.0` 文件夹；不要在该目录直接继续开发。
- 本目录可直接打开 `index.html` 使用，运行资源均为本地文件，不依赖联网加载。

## 本轮已完成

- 参数叉保持零厚度理想线段，并从第一帧让叉尖位于真实地面；叉子绕局部原点自由转动。
- CAD 武器与 CAD 叉使用三角复合实体，外轮廓继续接受独立的 `80 µm` 穿透审计。
- 参数齿的前角按最终装配后的物理正负定义，镜像不会把用户输入的正负含义反转。
- 参数武器、叉子、靶子与地面的持续多接触采用联立互补求解；已覆盖此前会停在靶子中的代表工况。
- “材料模型停止”“刚体求解停止”“内部异常停止”分开报告；任何失败步都会回滚到上一安全帧。
- 武器只填写总质量；转动惯量按当前 CAD/参数几何自动计算并只读显示。CAD 孔洞按奇偶规则扣除，参数齿先做多边形并集，避免重叠重复计质量。
- 数字栏可暂时清空再输入；清空草稿不会立即变红或触发重算。
- 仿真时长可在 `0.0005–4 s` 内选择，时间轴按 `0.5 ms` 固定步长工作；结果区显示靶子质心实时 X/Y 坐标。
- SIMD 可用时自动启用，初始化失败则回退普通 Rapier。
- 已验证的“理想刚体上限对照”改为默认接触模式，因此默认示例和测试齿/测试叉会继续运行；“有限材料研究模式”仍须由用户主动选择，缺少连续材料响应时继续安全回滚停止，不会静默借用刚体冲量。

## 尚未完成的物理能力

- 真正的连续剐蹭、塑性沟槽、断裂与删料尚未通过连续 TOI、历史材料表面和时间步收敛验证。“有限材料”首次承载接触会安全回滚并停止；“理想刚体上限”只能作为不可变形上界。
- 导入 CAD 武器仍由 Rapier 三角复合实体处理持续接触，尚未接入参数齿使用的外轮廓持久 MLCP 接触见证。
- 底盘只有水平自由度，不能预测整机俯仰、起跳、轮组垂向或落地姿态。
- 自动惯量假设完整旋转组件质量沿可见二维几何均匀分布；轮毂、轴、皮带轮和紧固件分布不同会产生偏差。
- 极复杂接触可能需要大量回滚与子步，虽不会假装成功，但预计算仍可能较慢。

更完整的适用范围与限制见 [README.md](README.md)。

## 关键回归

在项目根目录执行：

```powershell
node work/run-final-static-qa.js
node work/run-ui-final-qa.js
node work/run-automatic-weapon-inertia-qa.js
node work/run-param-runtime-qa.js
node work/run-p0-failure-domain-qa.js
```

卡靶物理代表工况使用 `work/run-final-independent-physics-audit.js`；长矩阵耗时较高，源码或接触公式有变化时再运行。

## 后续优先级

1. 为 CAD 武器建立严格外轮廓接触见证，并接入现有持久多接触求解，保持内部 Earcut 边不参与事件归因。
2. 将材料本构、永久沟槽/损伤历史、最早压缩 TOI 与余步重演纳入同一事务求解，再开放连续剐蹭。
3. 将轨迹预计算移入 Worker；在结果逐值一致的前提下继续优化复杂接触性能。
4. 若要定量判断整机击飞，扩展为底盘平移/升降/俯仰与轮地接触模型。
