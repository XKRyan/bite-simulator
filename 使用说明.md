# BITE LAB 武器咬入模拟器 v1.1

## 简介

BITE LAB 是一款浏览器端二维格斗机器人武器咬入模拟工具，用于观察旋转武器、叉子与靶子在不同几何、转速、传动比、质量和材料参数下的运动与接触过程。

程序可导入武器和叉子的 DXF 轮廓，也提供可调测试齿与测试叉。计算结果包括武器转速、刀尖速度、理论 bite、接触冲量、靶子实时坐标与速度、能量变化、几何侵入及求解状态。

## 使用方法

1. 解压后直接打开 `index.html`，无需安装。
2. 在左侧设置武器、叉子、行驶系统、靶子、材料和计算时长。
3. 如需使用自己的图纸，导入 ASCII DXF；DWG 请先在 CAD 软件中转换为 DXF。
4. 可切换真实 DXF 或测试几何，并设置镜像、位置、齿数、前角、尺寸和质量等参数。
5. 点击“播放”开始预计算。计算完成后可播放、暂停、单步或拖动时间轴查看任意时刻。
6. 中间画面用于观察接触过程，右侧显示实时运动、碰撞和能量结果。

只有内置 1.36 kg 示例自带起始武器/叉子质量。套用其他量级时，两项质量会清空并显示“待补质量”；填写当前实物的完整旋转组件质量和活动叉质量后，播放与单步才会启用。成功导入新的武器或叉子 DXF 也会清空对应质量，避免沿用内置部件数据。

数字输入框可以先全部清空再填写；空白内容在失焦时会恢复上一次有效值。靶子质量可按尺寸和密度自动计算，也可切换为手动输入。

## 适用范围

本工具适合：

- 比较不同武器直径、齿数、前角、初始角度和传动比；
- 比较叉子位置、武器高度及靶子尺寸对接触顺序的影响；
- 估算武器储能、刀尖速度、理论 bite 和二维刚体碰撞上限；
- 检查 DXF 几何、干涉、穿地和高转速接触中的数值稳定性；
- 用于概念设计、参数对比、教学演示和工程初筛。

## 模型边界

- 当前是二维侧视模型，不包含完整三维偏载、机身俯仰、整机跳起、轮胎悬架和结构振动。
- “理想刚体”结果是不可变形碰撞上限，不等同于真实材料切削或实际击飞距离。
- 连续切削、塑性沟槽、裂纹、碎片、热量和磨损尚未完成可靠验证。材料模式遇到超出可信范围的承载接触时会回滚并安全停止，不会用刚体冲量冒充材料结果。
- 结果依赖输入数据质量，仅适合比较和估算，不可替代实物测试、有限元分析或安全认证。

## 当前已知问题

1. **材料模式会在首次承载接触时安全停止。** 程序目前能识别武器与靶子的剐蹭接触，但尚不能可靠计算连续沟槽、塑性残余变形、硬化、断裂、去料以及后续齿反复经过同一区域的历史。停止前的末固定步会完整回滚，不提交临时刚体冲量、靶子速度或能量变化；这不是程序卡死。
2. **理想刚体模式可能高估击飞。** 该模式把靶子视为不可变形刚体，仅用于查看几何接触过程和碰撞上限。HDPE 等真实材料会通过塑性变形、刮削、断裂和摩擦消耗能量，因此实际击飞通常低于刚体结果。
3. **真实切削模型尚未完成。** 现有实验性材料计算缺少连续最早接触时间、与叉子和地面的同步联立求解、变形后表面历史及剩余时间重演，因此未在 v1.1 中启用。
4. **参数测试齿不能代表完整真实刀具。** 虽然其齿尖、前角和三角背撑有明确几何，但尚未定义微观刃圆、后角、断裂路径和实测最小切屑厚度，不能据此定量预测切削量或刀具失效。
5. **整机运动受到二维模型限制。** 底盘仅计算水平反冲，不包含竖向跳起、俯仰、轮胎悬架、重量转移和三维偏载，因此相关结果只能作为受限二维边界。
6. **完整切断和碎片尚不支持。** 靶子被切成多个独立部分时，需要多刚体断裂与碎片动量模型；当前版本不会继续伪算。
7. **DXF 支持范围有限。** 含 bulge、SPLINE、ELLIPSE、INSERT、三维 polyline 等不能无损解析的文件会被明确拒绝。请先在 CAD 中转换为 LINE、ARC 或无 bulge 的二维 polyline。
8. **计算仍可能较慢。** 高转速、复杂 CAD、贴近 0.08 mm 几何安全门或多点持续接触会触发更细的事务子步。SIMD 可提高速度，但计算时间仍取决于设备性能和所选时长。

这些限制不会通过速度限幅、经验冲量折扣或类似 PID 的调参隐藏。程序在超出已验证范围时优先回滚并明确停止。

v1.1 还会拒绝非零 Z/elevation/thickness/width、非 `+Z` extrusion、开放轮廓、自交轮廓、多个独立外环和嵌套孔。普通孔会保留为空腔并参与碰撞三角化和质量属性计算，不会被填实。
